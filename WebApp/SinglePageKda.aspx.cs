using System;

namespace SEOToolSet.WebApp
{
    public partial class SinglePageKda : System.Web.UI.Page
    {
        protected void Page_Load(object sender, EventArgs e)
        {
            //var CurrentMasterPage = this.Page.Master as BC;
            //if (CurrentMasterPage == null) return;
            //CurrentMasterPage.ShowSubmenu();
            //CurrentMasterPage.SetVariablesAccordingToToken("Analyze");
            //CurrentMasterPage.SetTitle(GetLocalResourceObject("PageResource.Title").ToString());
            //CurrentMasterPage.VariablesSetted = true;
        }
    }
}
